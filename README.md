# E-Commerce Public Dataset

## Setup Environment 

pip install Python
pip install NumPy
pip install Pandas
pip install Matplotlib
pip install Seaborn
pip install streamlit
pip install Plotly
pip install Babel

## run streamlit

streamlit run dashboard.py
